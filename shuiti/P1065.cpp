#include <bits/stdc++.h>

using namespace std;

int s[500] = {0};
struct informaiton
{
    int order;
    int cost;
} fde[25][25];

int machine[25][1005] = {0};

int flag[25] = {0};

int last_time[25] = {0};

int main()
{
    int n, m;
    scanf("%d %d", &n, &m);
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < m; j++)
        {
        }
    }

    return 0;
}